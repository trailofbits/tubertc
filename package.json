{
    "name": "tubertc",
    "version": "2.0.0",
    "description": "Tuber-time Communications",
    "author": "yying",
    "license": "MIT",

    "repository": {
        "type": "git",
        "url": "https://github.com/trailofbits/tubertc.git"
    },

    "keywords": [
        "node",
        "webrtc",
        "easyrtc",
        "video"
    ],

    "dependencies": {
        "easyrtc": "*",
        "express": "*",
        "nconf": "*",
        "socket.io": "1.0.*",
        "handlebars": "*"
    },

    "devDependencies": {
      "jsdoc": "3.3.3"
    },

    "engines": {
        "node": "5.x"
    },

    "scripts": {
        "doc": "./node_modules/.bin/jsdoc -r public --destination doc --readme README.md -c .jsdocrc",
        "lint": "eslint --ext .js -c .eslintrc .",
        "start": "node server.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
